<div class="{{ media }} box {{ service }}" data-id="{{ id }}">
  <div class="thumbnail">
    <img src="{{ icon }}">
  </div>

  <div class="caption">
    <span class="title" title="{{ title }}">{{ title }}</span>
    {{#if isLocal}}
      <a href="local/artist" class="artist" data-id="{{ artist }}" title="{{ artist }}">
        {{ artist }}
      </a>
    {{else}}
      <span class="artist" title="{{ artist }}">{{ artist }}</span>
    {{/if}}
  </div>

  <div class="right options-toggler">
    {{g 'option-vertical'}}
  </div>
</div>
